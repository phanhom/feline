<div align="center">

# Feline

**Agentic AI Coding Assistant**

[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)
[![VS Code](https://img.shields.io/badge/VS%20Code-Extension-green.svg)](https://code.visualstudio.com/)

</div>

Feline is an open-source, agentic AI coding assistant with intelligent code generation, multi-model LLM support, and autonomous tool execution. Features real-time streaming, context compression, task planning, session backtracking, and self-hosted or cloud deployment options.

## ✨ Features

- **Multi-LLM Support** - Works with multiple language model providers with automatic retry and fallback
- **Autonomous Tool Execution** - File operations, terminal commands, Git integration, and code search
- **Intelligent Context Management** - Automatic context compression to maximize conversation history
- **Plan Mode** - Structured task planning with user confirmation before execution
- **Session Management** - Conversation backtracking, forking, and history persistence
- **Real-time Streaming** - Live response streaming with thinking process visibility
- **Mention System** - Reference files with `@filename` and symbols with `#symbol`
- **Self-hosted or Cloud** - Run locally with your own API keys or use cloud services

## 🏗️ Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              User's Machine                                  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        VS Code Extension (feline-plugin)               │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────┐    │  │
│  │  │   feline    │  │    iris     │  │         whisker             │    │  │
│  │  │ (Extension  │  │  (Chat UI)  │  │    (Local Backend)          │    │  │
│  │  │   Host)     │  │   React     │  │  Python + FastAPI           │    │  │
│  │  └──────┬──────┘  └──────┬──────┘  └──────────────┬──────────────┘    │  │
│  │         │                │                        │                    │  │
│  │         └────────────────┼────────────────────────┘                    │  │
│  │                          │ Webview Message                            │  │
│  └──────────────────────────┼────────────────────────────────────────────┘  │
│                             │                                                │
└─────────────────────────────┼────────────────────────────────────────────────┘
                              │
                              │ HTTP/SSE
                              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Cloud Infrastructure                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    feline-server (API Gateway)                       │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐   │   │
│  │  │    Auth      │  │  LLM Proxy   │  │   Subscription/Usage     │   │   │
│  │  │   (JWT)      │  │  (Unified)   │  │      Management          │   │   │
│  │  └──────────────┘  └──────────────┘  └──────────────────────────┘   │   │
│  └──────────────────────────────────┬──────────────────────────────────┘   │
│                                     │                                        │
│  ┌──────────────┐  ┌──────────────┐ │ ┌──────────────┐  ┌──────────────┐   │
│  │  PostgreSQL  │  │    Redis     │◄┼►│ feline-monitor│  │ LLM Providers│   │
│  │  (Database)  │  │  (Cache)     │ │ │ (Dashboard)  │  │ OpenAI/Claude│   │
│  └──────────────┘  └──────────────┘ │ └──────────────┘  │ DeepSeek/... │   │
│                                     │                   └──────────────┘   │
│  ┌──────────────────────────────────┴──────────────────────────────────┐   │
│  │                    feline-deploy (Docker Compose)                    │   │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────────┐    │   │
│  │  │   Nginx    │ │ Prometheus │ │   Grafana  │ │   SSL/Certs    │    │   │
│  │  │ (Reverse)  │ │ (Metrics)  │ │ (Visualize)│ │   (Let's Encrypt)│   │   │
│  │  └────────────┘ └────────────┘ └────────────┘ └────────────────┘    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 📦 Project Structure

```
feline/
├── feline-plugin/     # VS Code extension (core product)
│   ├── packages/
│   │   ├── feline/    # Extension host (TypeScript)
│   │   ├── iris/      # Chat UI (React + Vite + Tailwind)
│   │   └── whisker/   # Local backend (Python + FastAPI)
├── feline-server/     # Cloud API server (Python + FastAPI)
├── feline-monitor/    # Monitoring dashboard (React + FastAPI)
└── feline-deploy/     # Docker deployment configuration
```

## 📋 Module Overview

### feline-plugin

**核心产品 - VS Code AI 编程助手扩展**

feline-plugin 是 Feline 的核心产品，是一个完整的 VS Code 扩展，采用 Monorepo 架构，包含三个子包：

| 子包 | 技术栈 | 职责 |
|------|--------|------|
| **feline** | TypeScript + Webpack | VS Code 扩展宿主，管理 Webview、会话持久化、与 Whisker 通信 |
| **iris** | React + Vite + Tailwind | 聊天 UI，运行在 Webview 中，负责消息渲染、工具展示、回溯功能 |
| **whisker** | Python + FastAPI | 本地后端服务，提供 LLM 接口、工具执行、上下文压缩 |

**核心功能：**
- 多 LLM Provider 支持（OpenAI、Claude、DeepSeek、智谱、Kimi、通义千问等）
- 自主工具执行（文件操作、终端命令、Git 集成、代码搜索）
- 智能上下文管理（自动压缩、Mention 系统）
- Plan Mode（结构化任务规划）
- 会话回溯与分叉
- 实时流式响应

**运行模式：**
- **本地模式**：用户自带 API Key，数据存储在本地
- **云端模式**：连接 feline-server，使用云端服务

---

### feline-server

**云服务后端 - 统一 LLM 网关与用户管理**

feline-server 是 Feline 的云服务后端，提供统一的 LLM 接口、用户认证、订阅管理和用量统计。

**核心功能：**
- **统一 LLM 接口**：标准化 API（`/v1/chat/completions`、`/v1/completions`）
- **用户系统**：注册/登录、JWT 认证、跨设备同步
- **订阅管理**：Free/Pro/Enterprise 三档订阅计划
- **用量统计**：Token 使用量追踪、按模型/提供商统计
- **速率限制**：基于 Redis 的请求频率限制

**技术栈：** FastAPI + SQLAlchemy 2.0 + PostgreSQL + Redis

**API 端点：**
- 认证：`/v1/auth/sign_up`、`/v1/auth/sign_in`、`/v1/auth/refresh_token`
- 推理：`/v1/chat/completions`、`/v1/completions`
- 用户：`/v1/accounts/me`、`/v1/subscriptions`、`/v1/usage/summary`

---

### feline-monitor

**监控面板 - 服务状态可视化**

feline-monitor 是 Feline 的监控面板，提供实时服务状态监控和数据分析。

**核心功能：**
- 服务健康检查
- Prometheus 指标可视化
- 用户活跃度分析
- Token 使用趋势

**技术栈：** React + FastAPI + Prometheus + Grafana

---

### feline-deploy

**部署配置 - Docker 容器化部署**

feline-deploy 提供 Feline 完整的 Docker 部署配置，支持一键部署所有服务。

**包含组件：**
- **Nginx**：反向代理、SSL 证书管理
- **PostgreSQL**：数据库
- **Redis**：缓存和速率限制
- **Prometheus**：指标收集
- **Grafana**：监控可视化

**部署命令：**
```bash
cd feline-deploy
cp .env.example .env
docker-compose up -d
```

---

## 🔗 Module Relationships

```
                    ┌─────────────────────────────────────┐
                    │           feline-plugin             │
                    │  ┌─────────┐ ┌───────┐ ┌─────────┐  │
                    │  │ feline  │ │ iris  │ │ whisker │  │
                    │  └────┬────┘ └───┬───┘ └────┬────┘  │
                    │       │          │          │       │
                    │       └──────────┼──────────┘       │
                    │                  │                  │
                    └──────────────────┼──────────────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    ▼                  ▼                  ▼
           ┌────────────────┐  ┌────────────────┐  ┌────────────────┐
           │  feline-server │  │ feline-monitor │  │ LLM Providers  │
           │  (Cloud API)   │  │  (Dashboard)   │  │ (OpenAI/Claude)│
           └───────┬────────┘  └───────┬────────┘  └────────────────┘
                   │                   │
                   ▼                   ▼
           ┌───────────────────────────────────────────────┐
           │              feline-deploy                     │
           │  ┌──────────┐ ┌────────┐ ┌────────┐ ┌───────┐ │
           │  │ Nginx    │ │ Postgres│ │ Redis  │ │Prom/Graf│
           │  └──────────┘ └────────┘ └────────┘ └───────┘ │
           └───────────────────────────────────────────────┘
```

**数据流：**

1. **本地模式**：
   ```
   feline (Extension) → whisker (Local) → LLM Provider
   ```

2. **云端模式**：
   ```
   feline (Extension) → feline-server (Cloud) → LLM Provider
   ```

3. **监控数据流**：
   ```
   feline-server → Prometheus → feline-monitor → Grafana
   ```

## 🚀 Quick Start

### Prerequisites

- Node.js >= 18
- Python >= 3.10

### Installation

```bash
git clone https://github.com/phanhom/feline.git
cd feline
npm run install:all
```

### Development

```bash
npm run dev:feline   # Extension development
npm run dev:iris     # UI development
npm run dev:whisker  # Backend development
```

### Build

```bash
npm run build           # Build all packages
npm run package:vsix    # Package VS Code extension
```

## 🐳 Docker Deployment

```bash
cd feline-deploy
cp .env.example .env
vim .env
docker-compose up -d
```

## 📖 Documentation

- [Plugin README](feline-plugin/README.md) - Extension usage and features
- [Server README](feline-server/README.md) - API documentation
- [Deploy README](feline-deploy/README.md) - Deployment guide
- [Developer Guide](developer.md) - Architecture and development guide

## 🤝 Contributing

Contributions are welcome! Please feel free to submit a Pull Request.

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

---

<div align="center">
Made with ❤️ by the Feline Team
</div>
